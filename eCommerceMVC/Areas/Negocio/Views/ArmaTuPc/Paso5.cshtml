@using eCommerce.Entities.ViewModels
@{
    ViewData["Title"] = "Arma tu PC - GPU";
    Layout = "~/Areas/Negocio/Views/Shared/_Layout.cshtml";
}

@model ArmaPcViewModel

@Html.Partial("Partials/_EstilosComunes")

<div class="arma-pc-container">
    <div class="container">
        <div class="row mb-4">
            <div class="col-12">
                <div class="text-white mb-3">
                    <a href="javascript:history.back()" class="text-white text-decoration-none">
                        <i class="fas fa-arrow-left me-2"></i>Volver Atrás
                    </a>
                </div>
                <h1 class="display-5 fw-bold text-white mb-2">@Model.Titulo</h1>
                <p class="text-white-50 mb-3">@Model.Descripcion</p>

                <div class="d-flex align-items-center gap-2 mb-2">
                    <div class="flex-grow-1 progress-wrapper">
                        <div class="progress-bar-custom" style="width: 62.5%;"></div>
                    </div>
                    <small class="text-white">Paso 5/8</small>
                </div>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-lg-8">
                <form method="post" action="@Url.Action("SeleccionarGpu")" id="formGpu">
                    <input type="hidden" name="idProcesador" value="@Model.IdProcesadorSeleccionado">
                    <input type="hidden" name="idMotherboard" value="@Model.IdMotherboardSeleccionado">
                    <input type="hidden" name="marca" value="@Model.MarcaSeleccionada">
                    @foreach (var idRam in Model.IdsRamSeleccionados ?? new List<int>())
                    {
                        <input type="hidden" name="idsRam" value="@idRam">
                    }

                    <div class="row g-3">
                        <!-- Opción: Sin GPU -->
                        <div class="col-md-6">
                            <label class="product-card">
                                <input type="radio" name="idGpu" value="" class="d-none" onchange="habilitarBoton()">

                                <div class="card h-100 border-0 shadow-sm">
                                    <div class="card-body p-3 text-center">
                                        <div class="mb-3" style="height: 180px; display: flex; align-items: center; justify-content: center;">
                                            <i class="fas fa-times-circle text-muted" style="font-size: 4rem;"></i>
                                        </div>
                                        <h6 class="fw-bold mb-2">Sin Tarjeta Gráfica</h6>
                                        <p class="text-muted small">Usar gráficos integrados del procesador</p>

                                        <div class="selection-indicator">
                                            <i class="fas fa-check-circle text-success" style="font-size: 1.2rem;"></i>
                                        </div>
                                    </div>
                                </div>
                            </label>
                        </div>

                        @foreach (var gpu in Model.GpusDisponibles)
                        {
                            <div class="col-md-6">
                                <label class="product-card">
                                    <input type="radio" name="idGpu" value="@gpu.IdProducto" class="d-none" onchange="habilitarBoton()">

                                    <div class="card h-100 border-0 shadow-sm">
                                        <div class="card-body p-3">
                                            <div class="text-center mb-3">
                                                @if (!string.IsNullOrEmpty(gpu.RutaImagen))
                                                {
                                                    <img src="@gpu.RutaImagen" alt="@gpu.Nombre" class="img-fluid" style="max-height: 180px; object-fit: contain;">
                                                }
                                                else
                                                {
                                                    <div class="d-flex align-items-center justify-content-center" style="height: 180px;">
                                                        <i class="fas fa-display text-muted" style="font-size: 4rem;"></i>
                                                    </div>
                                                }
                                            </div>

                                            <h6 class="fw-bold mb-2" style="min-height: 40px; font-size: 0.9rem;">@gpu.Nombre</h6>

                                            @if (gpu.Especificaciones.Any())
                                            {
                                                <div class="mb-3">
                                                    @foreach (var spec in gpu.Especificaciones.Take(2))
                                                    {
                                                        <small class="text-muted d-block" style="font-size: 0.75rem;">@spec.Valor</small>
                                                    }
                                                </div>
                                            }

                                            <div class="mb-2">
                                                <h5 class="mb-0 fw-bold">$@gpu.Precio.ToString("N0")</h5>
                                            </div>

                                            <div class="d-flex align-items-center justify-content-between">
                                                @if (gpu.EnStock)
                                                {
                                                    <small class="text-success"><i class="fas fa-check-circle me-1"></i>Compatible</small>
                                                }

                                                <div class="selection-indicator">
                                                    <i class="fas fa-check-circle text-success" style="font-size: 1.2rem;"></i>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </label>
                            </div>
                        }
                    </div>

                    <div class="d-lg-none mt-4">
                        <button type="submit" class="btn btn-light btn-lg w-100 fw-bold" id="btnSiguienteMobile" disabled>
                            Siguiente Paso <i class="fas fa-arrow-right ms-2"></i>
                        </button>
                    </div>
                </form>
            </div>

            <div class="col-lg-4">
                @Html.Partial("Partials/_ComponentesSidebar", Model)

                <div class="mt-3 d-none d-lg-block">
                    <button type="submit" form="formGpu" class="btn btn-gradient btn-lg w-100 fw-bold" id="btnSiguiente" disabled>
                        Siguiente Paso <i class="fas fa-arrow-right ms-2"></i>
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function habilitarBoton() {
        document.getElementById('btnSiguiente').disabled = false;
        const btnMobile = document.getElementById('btnSiguienteMobile');
        if (btnMobile) btnMobile.disabled = false;
    }
</script>